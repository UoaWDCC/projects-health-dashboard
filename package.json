{
  "name": "projects-health-dashboard",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "dotenv -e .env.dev -- pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml}\"",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test:watch",
    "test:coverage": "pnpm -r test:coverage",
    "db:generate:dev": "dotenv -e .env.dev -- pnpm --filter @repo/db db:generate",
    "db:generate:prod": "dotenv -e .env -- pnpm --filter @repo/db db:generate",
    "db:migrate:dev": "dotenv -e .env.dev -- pnpm --filter @repo/db db:migrate",
    "db:deploy:dev": "dotenv -e .env.dev -- pnpm --filter @repo/db db:deploy",
    "db:deploy:prod": "bash scripts/migrate-prod.sh",
    "db:studio:dev": "dotenv -e .env.dev -- pnpm --filter @repo/db db:studio",
    "db:studio:prod": "dotenv -e .env -- pnpm --filter @repo/db db:studio",
    "storage:setup:dev": "dotenv -e .env.dev -- tsx scripts/setup-storage.ts",
    "storage:setup:prod": "dotenv -e .env -- tsx scripts/setup-storage.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@types/node": "^25.5.0",
    "dotenv-cli": "^8.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.5.3",
    "tsx": "^4.21.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma",
      "sharp"
    ]
  }
}
