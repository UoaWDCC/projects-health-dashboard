# fly.toml for wphd-worker — weekly scheduled machine, no HTTP service
app = 'wphd-worker'
primary_region = 'syd'
schedule = 'weekly'

[build]
  dockerfile = 'Dockerfile.worker'

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
