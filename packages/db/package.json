{
  "name": "@repo/db",
  "version": "0.0.1",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "lint": "eslint src",
    "typecheck": "tsc --composite false --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prisma": "^5.22.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0"
  }
}
